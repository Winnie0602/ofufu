# 教材資料結構化規範（第三階段，給 ChatGPT）

## 使用方式：請先完整閱讀，再轉換核准內容

請將本訊息最下方的「第一階段核准內容」與「第二階段核准標註」依照本文件全部規則，轉成 Ofufu 指定格式的完整 TypeScript 物件。

這可能是一個全新的 session。**不要假設你記得前兩階段，也不要依賴本訊息以外的內容。**「第一階段核准內容」是唯一教材內容來源；「第二階段核准標註」是唯一重點單字／文法來源；本文件是唯一 TypeScript、Ruby、ID、examples 與 anchors 規範。

你的任務只有「結構化與教材標註」，不是重新創作：

1. 完全保留第一階段核准的類型、程度、情境、標題、簡介、角色、日文、繁中翻譯、段落、行數及順序。
2. 完全依照第二階段逐句核准的重點單字／文法，補上 TypeScript 結構、Ruby、ID、examples、anchors 及其他必要欄位，並一對一轉成 `vocabularyNotes`、`grammarNotes`。
3. 不得改寫、潤飾、增刪、合併、拆分或重新排列核准內容，也不得自行新增、刪除或替換重點單字／文法。
4. 若兩份核准輸入彼此不一致、與本規範衝突、資料不足、標註缺漏或疑似有錯，不要自行修改；停止轉換並列出需要人工確認的問題。
5. 若內容可安全轉換，最終只輸出一個可直接貼入 `app/data/materials/reading.ts` 或 `conversation.ts` 陣列的 TypeScript 物件，不輸出前言、分析或檢查過程。

> 第一階段規範位於 `docs/tasks/seed-content-draft.md`，第二階段規範位於 `docs/tasks/seed-annotation-draft.md`。第三階段不需要讀取或記得前兩個 session；兩份必要輸入都必須完整貼在本文件底部。

---

## 0. 你的角色與任務

你要把使用者貼上的**人工核准版本**轉成可直接貼進資料陣列的完整 TypeScript 物件，補上漢字讀音、符合程度的重點單字與文法、例句、anchors 及唯一 ID。所有新增的解釋與例句使用繁體中文及自然日文。

### 核准內容鎖定規則（最高優先）

1. 不得改寫、增刪或重新排序核准版本的標題、簡介、日文、繁中翻譯、角色、情境及核准候選單字／文法。
2. 閱讀段落、句子邊界及順序不得改變；對話行數、角色、發言內容及順序不得改變。
3. 不得合併或拆分句子、段落或對話行。
4. 加入 Ruby 後，把每個 `[...]` 移除，結果必須逐字等於核准日文。
5. TypeScript 內的逐句翻譯必須逐字等於核准翻譯；閱讀的段落翻譯只能由同段核准翻譯合併而成，不新增資訊。
6. 若發現核准內容疑似不自然、翻譯不符、難度不符或無法安全標註，只能列出問題並停止轉換，不得自行修正。
7. `vocabularyNotes` 只能來自核准的候選重點單字；`grammarNotes` 只能來自核准的候選重點文法。不得因模型判斷而多標、少標或換成別的項目。
8. 核准清單標示「無」時，對應 notes 必須省略；核准內容未提供候選清單時，停止轉換並要求補齊，不得自行推測。
9. 每個核准標註只能轉入它所在句子／對話行的 notes，不得跨句搬移、合併或重複建立。

### 核准標註的結構驗證原則

1. **只標「符合該程度」的重點單字與文法。**「該程度」指這篇教材的 `level`（n5～n1）。單字只標**這個程度該學、有學習價值**的重點詞；文法只標**這個程度才會教到**的句型。**不要標明顯低於該程度的基礎詞與基礎文法**（例：N2 文章不要把「去年」「一人」當重點單字，也不要標 `〜たり…する`、`〜てから` 這類 N5／N4 文法）。
2. **不得重新選擇標註。** 第二階段已負責程度與教學價值審稿；第三階段只驗證核准項目能否安全轉成合法型別、anchor 與 notes。即使認為還有其他可教項目，也不得自行新增。
> 輸出前務必回頭自我檢查（見 §10）：核准內容是否原封不動、是否混進低於程度的重點單字／文法、anchor 是否存在、Ruby 與 ID 是否正確。

### 單次產出數量與輸出長度

- **除非使用者在當次指令明確指定其他數量，否則一次只產 1 篇完整教材。** 不要自行改成多篇，也不要先詢問要完整版或精簡版。
- 一篇完整教材必須在**單一回覆**中輸出完畢，不得以規範較長、TypeScript 行數較多或可能超過回覆長度為由拒絕、拆次、刪除必要欄位或改成沒有註解的精簡版。
- `examples` 原則上每個重點單字提供 **1 句**即可；只有確有額外教學價值時才增加至 2～3 句。`grammarNotes` 只標實際符合程度且值得教的項目，不需湊數。
- 使用正常、緊湊且可讀的 TypeScript 排版。不要輸出分析、字數估算、自我檢查過程、id 清單、前言或其他說明，**最終只輸出可直接貼入資料陣列的 TypeScript 物件**。

---

## 1. 你要產出的結構

### 1-1. 閱讀教材（reading）

```ts
{
  id: 'V1StGXR8Z5jd',          // 模擬 id，見 §2
  type: 'reading',
  title: '日本の四季を楽しもう', // 純日文標題，不加 ruby；難度需符合 level
  excerpt: '桜や夏祭りについて読みます。', // 純日文簡介，不加 ruby；難度需符合 level
  level: 'n5',                  // n5 | n4 | n3 | n2 | n1，見 §7
  categories: ['culture', 'travel'], // 見 §7 合法值
  coverImage: null,             // 一律填 null（不提供圖片）
  paragraphs: [
    {
      id: 'RdP8kM2xQ5aN',
      sentences: [
        {
          id: 'RdS1wF7cL3pT',
          text: '日本[にほん]には四[よっ]つの季節[きせつ]があります。', // §3 括號記法
          translation: '日本有四個季節。', // 這一句的繁中翻譯
          vocabularyNotes: [ /* §5，可省略 */ ],
          grammarNotes: [ /* §6，可省略 */ ],
        },
        // …更多句子
      ],
    },
    // …固定 3 段、每段 2～3 句、合計 8～9 句，全文約 250～300 個純日文字元
  ],
  translation: [
    '第一段的繁體中文翻譯…',
    '第二段的繁體中文翻譯…',
    // paragraphs 有幾段，這裡就有幾個字串（整段層級翻譯）
  ],
}
```

### 1-2. 對話教材（conversation）

```ts
{
  id: 'C7Km3pQx92Ab',
  type: 'conversation',
  title: '電話で席を予約する', // 純日文標題，不加 ruby；難度需符合 level
  excerpt: 'レストラン予約の会話です。', // 純日文簡介，不加 ruby；難度需符合 level
  level: 'n4',
  categories: ['restaurant', 'reservation'],
  coverImage: null,
  sceneDescription: 'レストランに電話して席を予約する場面です。', // 日文情境說明
  participants: [
    { id: 'CvCh1mP8qR4tN', name: 'ふふ', role: '客人', avatarKey: 'fufu' },
    { id: 'CvCh2xK6vD9sL', name: 'シロ', role: '店員', avatarKey: 'shiro' },
  ],
  lines: [
    {
      id: 'CvLn1qT7mN3pH',
      speakerId: 'CvCh2xK6vD9sL', // 必須等於某個 participant 的 id
      text: 'お電話[でんわ]ありがとうございます。',
      translation: '感謝您的來電。',
      vocabularyNotes: [ /* §5，可省略 */ ],
      grammarNotes: [ /* §6，可省略 */ ],
    },
    // …共 10～12 行，每行原則 1～2 句
  ],
}
```

### 1-3. 標題、簡介與篇幅規則（重要）

- `title` 與 `excerpt` 都必須是**純日文**，不得使用 `漢字[かんじ]` 括號 Ruby、HTML 或繁體中文。
- 標題與簡介不是拿來測驗難字。請以**該篇 `level` 的學習者能直接理解**為優先，使用該程度已學過的字彙與文法；必要時寧可換成較簡單、自然的日文。
- 標題要簡潔並清楚說出主題；簡介用 1 句自然日文說明會讀到／練到什麼，不劇透全文，也不要寫成艱深的新聞標題。
- 閱讀：核准版本必須是**固定 3 段、每段 2～3 句、合計 8～9 句，全文約 250～300 個純日文字元**。字元數以 `text` 去除 `[...]` 讀音後的純日文計算，包含日文標點，不含標題、簡介、翻譯、單字例句與文法例句；第三階段不得為調整篇幅改寫內容。
- 對話：核准版本必須是**10～12 行、每行原則 1～2 句**，並具備自然的開場、任務展開與收尾；第三階段不得拆分、合併或新增對話行。

**對話固定角色**（只能用這三個，`name` 與 `avatarKey` 要配對）：
`ふふ`→`fufu`、`シロ`→`shiro`、`クロ`→`kuro`。`role` 是繁中身分（如「客人」「店員」）。

---

## 2. 模擬 id 規則

- 每個 `id`（教材、段落、句子、單字註解、文法註解、例句、角色）都是 **8～12 碼英數字**（`A–Z a–z 0–9`）的**無語意**字串，例：`RdS1wF7cL3pT`。
- **本次輸出內不可重複**。不要用有意義的字（不要 `sakura01`），不要用純數字流水號。
- **不要模仿本文件範例的字首規律**（例如範例裡同一篇教材底下的 id 都用 `Rd`／`Rx`／`Cv` 開頭）。那只是示範格式，不是要你照抄前綴；同一批輸出裡，各個 id 開頭的 1～2 碼盡量分散，避免大量 id 集中在同一字首附近而互撞。
- **輸出最終結果前，自行列出這次輸出用到的所有 id 並確認彼此不重複**（這份自我檢查清單不用放進貼給我們的程式碼裡，只是你產出前的內部檢查步驟）。
- 你只需保證「這一次輸出」內部不重複即可；**跨批次、與現有檔案的 id 撞號由專案方在貼回時自動處理**，你不必也無法顧到。
- `vocabularyItemId`、`grammarPointId` 等「連結 id」**你不要填**（見 §5／§6）。

---

## 3. Ruby 括號記法（最重要，務必照做）

內文（`text`）是**純文字**，漢字讀音用**半形中括號** `[ ]` 標在「緊接在前的連續漢字」後面。

### 規則
1. **只有半形 `[` `]` 是記法**。全形「［］」或其他括號會被當普通文字。
2. 括號內是**假名讀音**，掛在**它前面那段連續漢字**上。
3. **所有漢字都要標讀音**（初學者導向）；純假名不用標。
4. **送假名／夾在中間的假名要留在括號外**：
   - 動詞活用：`楽[たの]しむ`、`楽[たの]しめる`、`食[た]べる`
   - 夾假名複合詞：`食[た]べ物[もの]`
   - 訓讀數量詞：`四[よっ]つ`、`一[ひと]つ`
5. **純漢字詞可整組一個括號**：`季節[きせつ]`、`雪景色[ゆきげしき]`、`予約[よやく]`。
6. 若真的需要在文中打出半形 `[`，用反斜線跳脫：`\[`。

### 對照範例
| 想呈現 | 正確寫法 | 錯誤寫法 |
| --- | --- | --- |
| 日本（にほん） | `日本[にほん]` | `[にほん]日本` |
| 食べ物（たべもの） | `食[た]べ物[もの]` | `食べ物[たべもの]`（讀音不能蓋到假名） |
| 四つ（よっつ） | `四[よっ]つ` | `四つ[よっつ]` |
| 楽しめる | `楽[たの]しめる` | `楽しめる[たのしめる]` |

> **純日文** = 把每個 `[...]` 刪掉。系統用它做語音朗讀，所以 `text` 刪掉括號後必須是通順、正確的日文。

---

## 4. 句子／行結構

- `text`：帶括號記法的日文（§3）。
- `translation`：**這一句**的繁體中文翻譯。
- `vocabularyNotes?`／`grammarNotes?`：可省略；沒有可查單字或文法就不寫。

---

## 5. 單字註解 `vocabularyNotes`

標出句中**符合該程度的重點單字**，讓使用者可查、可收藏。**不要把每個實詞都標**；**助詞、標點不標**。

### 欄位

```ts
{
  id: 'RdVw8Tn3Lm2K',
  surface: '楽しめる',        // 句中實際出現的樣子（活用形）；見「anchor 規則」
  surfaceReading: 'たのしめる', // 「表層形」的讀音（全假名），對應畫面顯示
  dictionaryForm: '楽しむ',    // 辭書形（原形）
  reading: 'たのしむ',         // 辭書形讀音（全假名）
  partOfSpeech: 'verb',       // 見 §7 合法值
  contextualMeaning: '享受；此處為可能形，意為「能夠享受」', // 此字在本句的意思／用法（繁中）
  // occurrence: 2,           // 同一 surface 在本句出現多次時才寫（第幾個，1 起算）
  examples: [                 // 見下方「examples 填寫規則」，原則 1 句
    { id: 'RdVe5tK8mP2qL', japanese: '春には桜を楽しめる。', translation: '春天能享受櫻花。' },
  ],
}
```

### 該標哪些字（重要）
- **只標「符合該程度、有學習價值」的重點單字。**「該程度」＝本篇 `level`。挑這個程度該學、值得記的重點詞。
- **數量完全依第二階段核准結果。** 第三階段不得為達到任何數量自行新增或刪除；只需確認每個核准項目都能安全轉成合法結構。
- **不要標明顯低於程度的基礎詞。** 例：N2 文章不要把「去年」「一人」「毎日」這種 N5 就會的基礎詞當重點單字。
- **不要標字面＝解釋的瑣詞。** 若 `contextualMeaning` 幾乎就是把字翻成中文、沒有補充任何用法（例：「去年」→「去年」），代表這字沒有教學價值，**別標**。
- 偶爾標一個略高於本篇程度、但在本文很關鍵的難詞是可以的；重點是「值得學」，不是「湊數量」。

### 敬語結構中的片段不一定是獨立單字（重要）

- `surface` 能在句中定位，只代表系統找得到這段文字，**不代表它適合成為獨立的重點單字**。
- 不要把由動詞連用形、`お／ご` 接頭詞、名詞化或敬語結構在本句中暫時形成的片段，未經判斷就直接當成獨立詞彙。這不是宣稱所有 `お＋動詞連用形` 都不能作名詞；必須依實際句意、辭典中的詞彙成立性與本句用法逐一判斷。
- 每個 `vocabularyNotes` 都必須**同時**通過以下檢查：
  1. `surface` 能自然連回正確的 `dictionaryForm`。
  2. `partOfSpeech` 符合該詞在本句中的實際用法。
  3. 該項目本身確實可作為獨立詞彙學習，而不是只截取敬語結構、活用或名詞化中的一段。
  4. 該詞符合本篇 JLPT `level`。
  5. 該詞在本句中有足夠的教學價值。
- 若無法同時滿足「表層定位自然、辭書形正確、詞性合理、可獨立成立、具有學習價值」，就**不要標註**。
- 若真正值得教的是整體服務敬語或固定表達，應考慮以 `grammarNotes` 標整體句型，不要勉強建立單字註解。

句子：`お荷物はお預けになりますか。`

❌ 不建議：

```ts
surface: 'お預け'
dictionaryForm: '預け'
partOfSpeech: 'noun'
```

原因：「お預け」在此是動詞 `預ける` 的連用形配合敬語結構形成的表層片段，不宜直接視為獨立名詞；「預け」也不是真正的辭書形。

較佳處理：

- 不標成 `vocabularyNotes`；或
- 若要教詞彙，確認能否以 `預ける` 為核心自然設計標註，且 `surface` 仍須能在原句中正確定位；或
- 將 `お預けになりますか` 視為服務業固定表達／文法重點。

### `examples` 填寫規則
- 目前教材尚未串接完整單字表（`vocabularyItemId` 未填），畫面上單字彈窗的例句**只能來自這裡**。
- 既然標出來的都是重點字，**原則上各附 1 句 `examples`**；只有不同例句能補充重要語感或用法時才增加至 2～3 句（畫面 popover 最多顯示 3 句）。真的沒有合適例句可省略（畫面會優雅降級、不留空框）。

### anchor 規則（務必遵守，否則系統定位不到）
- `surface` 必須是「**把 `text` 的括號刪掉後的純日文**」裡**逐字存在的子字串**。
  - 例：`text: '楽[たの]しめるようになります。'` → 純日文 `楽しめるようになります。` → `surface` 可以是 `楽しめる`（存在），不可是 `楽しむ`（不在句中）。
- `surface` **本身不含括號**（是純日文），讀音改放在 `surfaceReading`。
- 同一 `surface` 在同一句出現多次時，用 `occurrence` 指定第幾個（1 起算）；只出現一次就省略。

### 表層形 vs 辭書形
- `surface`／`surfaceReading`：句中**實際樣子**與其讀音（初學者看到的）。
- `dictionaryForm`／`reading`／`partOfSpeech`：**辭書形自然鍵**，系統用來配對單字表、收藏去重。
- 名詞等原形＝表層形時，`surface` 與 `dictionaryForm` 可相同。

---

## 6. 文法註解 `grammarNotes`

標出句中**符合該程度的文法句型**。

```ts
{
  id: 'RdGr7Yp2Lm9Q',
  grammarPointId: null,        // 一律 null（系統之後自動連結，你不要填）
  pattern: '〜てくる',          // 文法句型（前面加「〜」表接續）
  shortMeaning: '漸漸變得～',    // 簡短意思，寫在標題旁；見下方規則
  explanation: '某種變化從過去一點一點累積、發展到現在，帶有時間推移的語感。', // 補充說明；見下方規則
  anchors: [                   // 句中要框起來的關鍵字，見規則
    { surface: '慣れてくる' },
  ],
  sourceExample: {             // 取自本句的代表例句（japanese 用純日文，不加括號）
    id: 'RdGe1qM6xP4nH',
    japanese: '生活に少し慣れてくる。',
    translation: '漸漸習慣了生活。',
  },
  extraExample: {              // 另一個同句型例句
    id: 'RdGe2kV9tC5sL',
    japanese: '寒くなってきました。',
    translation: '天氣漸漸變冷了。',
  },
}
```

### 該標哪些文法（重要）
- **只標「本篇程度才會教到」的文法。**「該程度」＝本篇 `level`。
- **不要標明顯低於程度的基礎文法。** 例：N2 文章不要標 `〜たり…する`、`〜てから`、`〜のが好き` 這類 N5／N4 句型；那些對 N2 學習者沒有學習價值。標到不合程度的文法，跟標錯重點單字一樣是要避免的錯誤。
- 稀疏即可，一句頂多標 1～2 個文法點。

### `shortMeaning` 與 `explanation` 兩欄位（重要，不要寫成公式）
- **`shortMeaning`（簡短意思）**：一句話的短 gloss，寫在彈窗／列表的標題旁，讓人一眼看懂這個句型「大概是什麼意思」。例：`〜てくる`→`漸漸變得～`、`〜ものの`→`雖然～但是`、`〜をきっかけに`→`以～為契機`。**不要**在這裡寫「表示…」這種前綴，直接給對應的中文說法即可。
- **`explanation`（補充說明）**：較長、給人看得懂的說明，講清楚語感、使用情境或跟相近句型的差別。
- **不要每一條都寫成「表示ＸＸＸ，意思是「ＸＸＸ」。」的公式。** 用比較自然、有意義的說法。
  - ❌ 不好：「表示某種變化從過去逐漸發展到現在，可譯為「漸漸變得～」。」
  - ✅ 較好：`shortMeaning: '漸漸變得～'`，`explanation: '某種變化從過去一點一點累積、發展到現在，帶有時間推移的語感。'`

### anchors 規則
- 每個 `anchor.surface` 同樣必須是「`text` 刪括號後純日文」裡的**子字串**（同 §5 anchor 規則），且**不含括號**。
- **只框關鍵字**，不要把整句都框起來。
- **不連續文法**（關鍵字被逗號或其他字隔開，如 `〜たり、〜たりする`）：用**多個 anchor**，例：
  ```ts
  anchors: [{ surface: '眺めたり' }, { surface: '温めたり' }]
  ```
- `sourceExample.japanese`、`extraExample.japanese` 用**純日文**（不加括號記法）。

---

## 7. 合法列舉值（只能用這些）

- `level`：`'n5' | 'n4' | 'n3' | 'n2' | 'n1'`
  - **各級語感（決定題材、詞彙與句子的難度質感）**：
    - **N5**：語感像小學生，最基本的生活用語、簡短句子。
    - **N4**：旅行、日常生活、打工等貼近生活的場景。
    - **N3**：新聞、職場等稍正式的內容。
    - **N2**：社會議題、較抽象的概念。
    - **N1**：評論、深入分析等高階內容。
- `partOfSpeech`：`'noun'`（名）、`'verb'`（動）、`'i_adjective'`（い形）、`'na_adjective'`（な形）、`'adverb'`（副）、`'particle'`（助）。
  - 標註以**實詞**為主，`particle` 幾乎用不到。
- `categories`（用這些 key）：
  `news, science, culture, food, travel, life, restaurant, reservation, airport, work, schedule, hotel, shopping, service, daily, friends`
  - **最多 2 個**，挑**最貼近內容的兩個**即可（也可以只有 1 個）。不要為了多貼標籤而硬湊到 3 個以上。
- `avatarKey`（對話角色）：`'fufu' | 'shiro' | 'kuro'`，對應 `name`：`ふふ / シロ / クロ`。

---

## 8. 禁止事項

1. **不要**用全形括號當讀音記法；讀音只用半形 `[ ]`。
2. **不要**把讀音蓋到假名上（`食べ物[たべもの]` 錯，要 `食[た]べ物[もの]`）。
3. `surface`／`anchor.surface`／各 `examples.japanese`／`sourceExample.japanese` **不含括號記法**（純日文）。
4. `surface`／`anchor.surface` 必須是該句純日文的**真實子字串**，否則系統定位失敗。
5. **不要**填 `vocabularyItemId`；`grammarPointId` 一律 `null`。
6. `partOfSpeech`、`level`、`categories`、`avatarKey` 只能用 §7 列的值；`categories` 最多 2 個。
7. **不要**把每個實詞都標成重點單字，也**不要**標明顯低於程度的基礎詞（如 N2 的「去年」「一人」）或字面＝解釋的瑣詞。
8. **不要**標明顯低於程度的基礎文法（如 N2 標 `〜たり…する`、`〜てから`）。
9. **不要**把 `explanation` 都寫成「表示ＸＸＸ，意思是「ＸＸＸ」。」的公式；`shortMeaning` 只寫短 gloss、不加「表示」前綴。
10. `id` 不重複、無語意、8～12 碼英數。
11. **不要**輸出 HTML／`<ruby>` 標籤；ruby 只用括號記法。
12. `text` 刪掉括號後必須是**通順正確的日文**（供朗讀）。
13. `title`／`excerpt` 必須是純日文、沒有 ruby，並以該 `level` 學習者看得懂的表達為優先。
14. 閱讀固定 3 段、每段 2～3 句、合計 8～9 句、約 250～300 個純日文字元；對話為 10～12 行、每行原則 1～2 句。第三階段不得為符合篇幅修改核准內容。
15. **不要**把敬語接頭詞或名詞化片段（如 `お預け`、`ご案内`、`お会計`）當獨立重點單字；優先標能對應辭書形的詞，否則改標文法／固定表達。
16. **不要**為了提高敬語程度而擅自補入可由情境推知的詞（如 `ご出発`、`ご利用`、`お手続き`）；簡潔的慣用說法優先。

---

## 9. 完整範例（可照抄結構）

```ts
// 閱讀，一段兩句
{
  id: 'RxN7aM2kP9qd',
  type: 'reading',
  title: '朝の公園',
  excerpt: '朝の公園の様子を読みます。',
  level: 'n5',
  categories: ['life', 'daily'],
  coverImage: null,
  paragraphs: [
    {
      id: 'RxP1aa2bb3cc',
      sentences: [
        {
          id: 'RxS1dd4ee5ff',
          text: '朝[あさ]の公園[こうえん]は静[しず]かで、犬[いぬ]を散歩[さんぽ]させる人[ひと]がいます。',
          translation: '早晨的公園很安靜，有人在遛狗。',
          vocabularyNotes: [
            {
              id: 'RxV1gg6hh7ii',
              surface: '静か',
              surfaceReading: 'しずか',
              dictionaryForm: '静か',
              reading: 'しずか',
              partOfSpeech: 'na_adjective',
              contextualMeaning: '安靜的',
              examples: [
                { id: 'RxV3aa1bb2cc', japanese: '夜の町はとても静かだ。', translation: '夜晚的城鎮非常安靜。' },
              ],
            },
            {
              id: 'RxV2jj8kk9ll',
              surface: '散歩',
              surfaceReading: 'さんぽ',
              dictionaryForm: '散歩',
              reading: 'さんぽ',
              partOfSpeech: 'noun',
              contextualMeaning: '散步；此處為「散歩させる（帶去散步／遛）」',
              examples: [
                { id: 'RxV4dd3ee4ff', japanese: '毎朝、犬の散歩をします。', translation: '每天早上都會遛狗。' },
              ],
            },
          ],
        },
        {
          id: 'RxS2mm0nn1oo',
          text: '天気[てんき]が良[よ]い日[ひ]は、子[こ]どもたちが遊[あそ]んだり、走[はし]ったりします。',
          translation: '天氣好的日子，孩子們會玩耍、奔跑。',
          grammarNotes: [
            {
              id: 'RxG1pp2qq3rr',
              grammarPointId: null,
              pattern: '〜たり、〜たりする',
              shortMeaning: '又是～又是～',
              explanation: '從幾個動作中舉出兩三個代表，表示「做做這個、做做那個」，不把所有事都列完。',
              anchors: [{ surface: '遊んだり' }, { surface: '走ったり' }],
              sourceExample: {
                id: 'RxE1ss4tt5uu',
                japanese: '子どもたちが遊んだり、走ったりします。',
                translation: '孩子們會玩耍、奔跑等。',
              },
              extraExample: {
                id: 'RxE2vv6ww7xx',
                japanese: '休みの日は本を読んだり、音楽を聞いたりします。',
                translation: '休假時會看看書、聽聽音樂。',
              },
            },
          ],
        },
      ],
    },
  ],
  translation: [
    '早晨的公園很安靜，有人在遛狗。天氣好的日子，孩子們會在公園裡玩耍、奔跑。',
  ],
}
```

---

## 10. 送出前自我檢查清單

- [ ] **核准內容完全保留**：標題、簡介、日文、逐句繁中翻譯、角色、情境、段落、句序、對話行及核准候選單字／文法均未改寫、增刪或重排。
- [ ] **標註一對一**：每個 `vocabularyNotes`／`grammarNotes` 都能對回一個核准候選項目，沒有自行新增、刪除或替換。
- [ ] **標註沒有跨句**：每個 note 都建立在候選項目原本所屬的句子／對話行，沒有搬到其他位置。
- [ ] **疑問沒有被擅自修正**：若核准內容存在自然度、敬語、真實流程、難度或翻譯疑問，已停止轉換並列出問題。
- [ ] 每個 `text` 刪掉 `[...]` 後是通順正確的日文；所有漢字都有讀音。
- [ ] 每個 `text` 刪掉 `[...]` 後逐字等於核准日文；每個 `translation` 逐字等於核准翻譯。
- [ ] 讀音沒有蓋到假名（送假名留在括號外）。
- [ ] 每個 `surface` 與 `anchor.surface` 都是該句純日文的真實子字串、且不含括號。
- [ ] **重點單字完全依核准標註**：沒有自行新增、刪除或替換；每個項目都能轉成合法詞性、辭書形與 anchor。
- [ ] **單字成立性**：每個重點單字不只是在句中找得到，也能合理對應正確辭書形與詞性，並且確實適合作為獨立詞彙學習；沒有把敬語構造中的表層片段誤當成單字。
- [ ] **文法都符合本篇 `level`**：沒有標明顯低於程度的基礎文法（如 N2 標 `〜たり…する`、`〜てから`）。
- [ ] 每個文法都有 `shortMeaning`（短 gloss、無「表示」前綴）與 `explanation`（自然、有意義，沒有寫成「表示ＸＸＸ，意思是「ＸＸＸ」。」的公式）。
- [ ] 重點單字大多有附上 1 句 `examples`；只有確有額外教學價值時才增加至 2～3 句。
- [ ] `examples`／`sourceExample`／`extraExample` 的 `japanese` 都是純日文（無括號）。
- [ ] `title`／`excerpt` 是純日文、沒有 ruby，且用字與句型符合本篇 `level` 學習者可理解的程度。
- [ ] 沒填 `vocabularyItemId`；所有 `grammarPointId` 為 `null`。
- [ ] `level`／`partOfSpeech`／`avatarKey` 都用合法值；`categories` 為最貼近的 **1～2 個**。
- [ ] **篇幅與邊界未改變**：閱讀固定 3 段／每段 2～3 句／合計 8～9 句／含標點約 250～300 個純日文字元；對話 10～12 行／每行原則 1～2 句。第三階段沒有拆分、合併或新增內容。
- [ ] 對話 `speakerId` 都對得上某個 `participant.id`。
- [ ] 所有 `id` 皆 8～12 碼英數、無語意、不重複；已依 §2 自我檢查過整批 id 清單。
- [ ] `reading` 段落翻譯數量 = `paragraphs` 段落數。

### 最終第二輪校對（完成後再做）

完成結構化後，先不要立刻輸出。請再以「**資料轉換校對者**」與「**日語詞彙編輯**」的角度檢查：

1. 移除全部 Ruby 後，逐句比對是否完全等於核准日文。
2. 逐句比對繁中翻譯、角色、順序及段落邊界是否完全等於核准版本。
3. 檢查每個 `vocabularyNotes` 是否真的是可獨立成立的詞彙，而不是敬語結構、活用或名詞化形成的片段。
4. 檢查 `dictionaryForm` 是否為真正的辭書形，`partOfSpeech` 是否合理。
5. 結構或標註問題可以直接修正；核准內容有問題時停止輸出並回報，不得自行改寫。

---

## 附錄：貼回流程備註（給專案方，非 ChatGPT 需遵守）

> 這段是給專案方的工作流程，ChatGPT 不必負責；第三階段規範需連同兩份完整核准結果貼給 ChatGPT。

1. **兩次核准再轉換**：先使用 `seed-content-draft.md` 產生並核准內容，再使用 `seed-annotation-draft.md` 產生並核准逐句標註；最後把兩份完整核准結果貼入本文件，一次只轉換 1 篇。
2. **貼回位置**：把物件貼進 `app/data/materials/reading.ts` 或 `conversation.ts` 的**陣列 `[ ]` 內**，與現有資料並排、逗號隔開；不覆蓋現有代表案例；`import` 那行不動。
3. **跨批 id 撞號屬正常**：ChatGPT 分批產出常重用 id。貼回後由專案方以腳本自動 dedupe（保留每個 id 第一次出現、重複者換新唯一值），不需人工管。
4. **貼回後必驗**（專案方執行）：依 §10 送出前清單逐項檢查，最後跑 `npx eslint <changed-files>` ＋ `npm run build`。

## 附錄：三階段搬運對照

> 本表用來確認舊版單一 Prompt 的每一類規則都有新位置，沒有在拆分時靜默遺失。

| 舊版內容 | 新位置 |
| --- | --- |
| 角色與任務、母語自然度、日本劇本編輯定位 | `seed-content-draft.md` §0 |
| 教材品質優先順序 | `seed-content-draft.md` §0 |
| 篇幅、不要硬湊字數、單次產出數量 | `seed-content-draft.md` §0 |
| 服務業敬語與場景程度 | `seed-content-draft.md` §1 |
| 日本真實流程與保守原則 | `seed-content-draft.md` §1 |
| 自然優先、不硬塞文法、不過度敬語化 | `seed-content-draft.md` §1 |
| 對話連貫、角色一致、避免重複與 AI 味 | `seed-content-draft.md` §1 |
| JLPT 各級內容質感 | `seed-content-draft.md` §2；合法 `level` 值仍保留於本文件 §7 |
| 標題、簡介與角色的內容規則 | `seed-content-draft.md` §2；結構欄位與固定角色 mapping 保留於本文件 §1 |
| 第一階段人工可讀內容格式 | `seed-content-draft.md` §3 |
| 第一階段禁止標註與程式資料 | `seed-content-draft.md` §4 |
| 母語、敬語、流程、連貫、難度與翻譯檢查 | `seed-content-draft.md` §5 |
| 候選重點單字／文法選擇、合法詞性與人工核准 | `seed-annotation-draft.md` §0～§4 |
| 標註程度、價值、表層定位及跨句檢查 | `seed-annotation-draft.md` §1～§5 |
| Reading／Conversation TypeScript 結構 | 本文件 §1 |
| 模擬 ID | 本文件 §2 |
| Ruby 括號記法 | 本文件 §3 |
| 句子／行結構 | 本文件 §4 |
| 重點單字、敬語片段、examples、anchor、辭書形 | 本文件 §5 |
| 文法、shortMeaning、explanation、anchors | 本文件 §6 |
| 合法列舉值 | 本文件 §7 |
| TypeScript 禁止事項 | 本文件 §8 |
| 完整 TypeScript 範例 | 本文件 §9 |
| Ruby、notes、anchor、ID、型別及結構自我檢查 | 本文件 §10 |
| 原本允許結構化階段直接修改原文的指令 | 第一階段可在 `seed-content-draft.md` §5 修正草稿；第二、三階段遇到核准內容疑問就停止並回報 |
| 貼回位置、跨批 ID 去重、ESLint 與 build | 本文件「貼回流程備註」 |

---

## 第一階段核准內容（每次使用時貼在這裡）

> 將 `seed-content-draft.md` 產生並經人工確認的完整內容貼在兩個標記之間。這份內容不包含重點單字與文法。

```text
【第一階段核准內容開始】

（在此貼上完整核准內容）

【第一階段核准內容結束】
```

## 第二階段核准標註（每次使用時貼在這裡）

> 將 `seed-annotation-draft.md` 產生、經人工刪改並確認的完整逐句標註版本貼在兩個標記之間。即使與前兩階段位於同一個 session，也必須重新貼上兩份完整核准結果。

```text
【第二階段核准標註開始】

（在此貼上含逐句重點單字／文法的完整核准版本）

【第二階段核准標註結束】
```
